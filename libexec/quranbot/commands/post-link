#!/bin/sh
set -e

##
# vars
libexec=$(realpath "$(dirname "${0}")"/..)

##
# main
for url in $(deno run --allow-read "${libexec}"/scripts/get-urls.ts); do
    deno run \
	 --allow-read \
	 --allow-env \
	 --allow-net \
	 "${libexec}"/scripts/post-link.ts "${url}"
done
