#!/bin/sh
set -e

##
# vars
libexec=$(realpath "$(dirname "${0}")"/../libexec/quranbot)

##
# main
for url in $(deno run --allow-read "${libexec}"/scripts/get-urls.ts); do
    deno run \
	 --allow-read \
	 --allow-env \
	 --allow-net \
	 "${libexec}"/scripts/post-reminder.ts "${url}"
done
